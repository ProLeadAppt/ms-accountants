/** Single source of truth for the hero still (used by the hero poster + preloader). */
export const HERO_STILL = "/generated/hero-study-hires.jpg";

/** Ambient hero loop (drifting dust/light) generated from HERO_STILL via Kling i2v.
 *  Reduced-motion users never load this; they get HERO_STILL instead. */
export const HERO_VIDEO = "/generated/hero-study-hires.mp4";
